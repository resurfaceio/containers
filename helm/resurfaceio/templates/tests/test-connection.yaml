apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resurfaceio.fullname" . }}-test-connection"
  labels:
    {{- include "resurfaceio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resurfaceio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
