Thank you for installing {{ .Chart.Name | title }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}
  
You can get the API Explorer URL by running the following command:

echo http://$(kubectl get svc --namespace {{ .Release.Namespace }} coordinator --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}"){{ if ne (int .Values.service.apiExplorer.port) 80 }}:{{ .Values.service.apiExplorer.port }}{{ end }}/
